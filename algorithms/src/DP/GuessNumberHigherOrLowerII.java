package DP;

public class GuessNumberHigherOrLowerII {
    public int getMoneyAmount(int n) {
        int res = 0;

        int start = 1, end = n;

        while(start < end) {
            int mid =  start + (end - start) / 2;
        }
        return 0;
    }
}
